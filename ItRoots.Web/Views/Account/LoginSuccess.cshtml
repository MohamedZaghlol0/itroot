<h2>Login Success</h2>
<p>Here is your JWT token (for dev):</p>
<div style="word-wrap: break-word; max-width: 600px;">
    @ViewBag.Token
</div>
<p>Use it as <code>Authorization: Bearer &lt;token&gt;</code> in requests.</p>
